# Copy to .env and edit:  cp .env.example .env
#
# === LLM (required for generated answers — pick one path) ===
#
# A) Local Ollama (free, no OpenAI bill) — https://ollama.com
#    Run: ollama serve   then: ollama pull llama3.2
USE_OLLAMA=1
OPENAI_MODEL=llama3.2
# OPENAI_BASE_URL=http://127.0.0.1:11434/v1
#
# B) Cloud / OpenAI-compatible API — set USE_OLLAMA=0 and:
# USE_OLLAMA=0
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# === Database (assessment: use a real DB when submitting) ===
# Uncomment after: docker compose up -d
# DATABASE_URL=postgresql://vectera:vectera@localhost:5432/vectera
#
# If DATABASE_URL is unset, the app uses SQLite + FAISS under ./data/ (no Docker).

# EMBEDDING_DIM=384
